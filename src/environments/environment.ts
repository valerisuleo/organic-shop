// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
        apiKey: "AIzaSyCOqWcUyqJnHKxdXlZluXoYoWkpabbTbdA",
        authDomain: "ng-mosh-project.firebaseapp.com",
        databaseURL: "https://ng-mosh-project.firebaseio.com",
        projectId: "ng-mosh-project",
        storageBucket: "ng-mosh-project.appspot.com",
        messagingSenderId: "465076808497",
        appId: "1:465076808497:web:cd57682381f2fcd6191e10",
        measurementId: "G-1DTSBVVV91"
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
